import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    // Фотосъёмка ещё не передана. Когда появятся внешние источники
    // (CDN клиента или headless-бэкенд), хосты добавляются сюда — без этого
    // next/image их не отдаст.
    remotePatterns: [],

    // AVIF идёт первым осознанно: на карточке каталога он весит вдвое меньше
    // webp при той же картинке (замер на двенадцати кадрах — 564 КБ против
    // 1024 КБ). Браузеры без поддержки получают webp следующим в списке.
    formats: ["image/avif", "image/webp"],

    // Длинная сторона подготовленной съёмки — 1600px, поэтому ступени 2048 и
    // 3840 из набора по умолчанию бессмысленны: оптимизатор не увеличивает
    // кадр и вернул бы те же 1600px, но отдельной записью кэша и отдельным
    // кодированием. Каждая лишняя ступень — это ещё один холодный AVIF на
    // первого посетителя.
    deviceSizes: [640, 750, 828, 1080, 1200, 1920],

    // Мелкие ступени нужны ровно двум местам: миниатюрам галереи (80–96px) и
    // строкам корзины (48px). Остальные из набора по умолчанию не запрашивает
    // никто.
    imageSizes: [48, 96, 256, 384],

    // Файлы лежат в репозитории и меняются вместе с деплоем, поэтому
    // переоптимизировать их раз в четыре часа (умолчание) незачем.
    minimumCacheTTL: 60 * 60 * 24 * 31,
  },
};

export default nextConfig;
